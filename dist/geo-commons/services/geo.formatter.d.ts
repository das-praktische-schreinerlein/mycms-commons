export declare class GeoFormatter {
    static humanLen(l: any): string;
    static formatMToKm(l: number): number;
    static formatM(l: number): number;
    static formatMillisToHH24(l: number): number;
}
